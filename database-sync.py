# Database helper file.
# If the database is empty or not in sync with the current schema,
# Running this script re structurs the database


# this script is to be run only after docker compose up via docker teminal

if __name__ == "__main__":
    print("RUNNING DATABASE SYNC SCRIPT")